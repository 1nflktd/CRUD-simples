# CRUD-Simples

- Banco de dados:

	CREATE DATABASE crud_simples;

- Tabela a ser criada:

	CREATE TABLE `filme` (
		`id` INT AUTO_INCREMENT,
		`nome` VARCHAR(100) NULL,
		`ano` INT NULL,
		PRIMARY KEY (`id`)
	);